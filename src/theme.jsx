import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            main: '#1976D2',
        },
        secondary: {
            main: '#FF5722',
        },
        customColor: {
            main: '#00FF00',
        },
    },
});

export default theme;
